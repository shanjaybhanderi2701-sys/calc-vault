package com.appblish.calculatorvault.ui.theme

import androidx.compose.material3.Typography

// Default Material3 type scale for now. Replace with the wireframe's type ramp
// (APP-142) once approved; kept as a single hook so screens don't hardcode styles.
internal val CalculatorVaultTypography = Typography()
